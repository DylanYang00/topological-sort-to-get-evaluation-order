#pragma once
#include <string>
using namespace std;

const string expFile = "exp.txt"; // file to read expressions from
const string edgeFile = "edge.txt"; // temp file resulted from the expression file and consists of edges